// Generated by CoffeeScript 1.10.0
(function() {
  var AlertView,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  AlertView = (function(superClass) {
    extend(AlertView, superClass);

    function AlertView() {
      return AlertView.__super__.constructor.apply(this, arguments);
    }

    AlertView.prototype.events = {
      'click .clear-alert-btn': 'hideAlert'
    };

    AlertView.prototype.showAlert = function(alertData) {
      var buttons, message, showCloseBtn;
      if (alertData) {
        message = alertData.message || '';
        buttons = alertData.buttons || '';
        showCloseBtn = alertData.showCloseBtn || false;
        if ((showCloseBtn != null) && showCloseBtn === true) {
          this.$('.clear-alert-btn').show();
        } else {
          this.$('.clear-alert-btn').hide();
        }
        if (this.$('.alert-buttons').length > 0 && !this.$el.hasClass('keep-alert')) {
          this.$('.alert-buttons').remove();
        }
        if (this.$('.alert-text').length === 0) {
          this.$('.alert').append('<div class="alert-text"></div>');
        }
        if (this.$('.alert-buttons').length === 0 && buttons !== '') {
          this.$('.alert').append('<div class="alert-buttons"></div>');
        }
        this.$('.alert-text').html(message);
        this.$('.alert-buttons').append(buttons);
      }
      if (this.$('.alert-text').length > 0) {
        return this.$el.removeClass('keep-alert').show();
      }
    };

    AlertView.prototype.hideAlert = function() {
      this.$el.hide();
      this.$('.alert-text').html('');
      return this.$('.alert-buttons').html('');
    };

    return AlertView;

  })(Backbone.View);

  window.alertView = new AlertView({
    el: $('#alert-wrapper')
  });

}).call(this);

//# sourceMappingURL=alert-view.js.map
